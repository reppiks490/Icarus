# Icarus goal (durable) — Grok (xAI) 2026-09-22

This file **is** the goal. Delete the live Ultraglobal / credit-burning prompt.
Agents start here + `ASTRA_DO_NOT.md` + `ENGINE_UNIVERSE_GROK.md` + `CANDIDATE_COG_GROK.md` + `TRAINERS_GROK.md`.

The video (`20260922-0358-29…mp4`) is illustration. The screenshots recovered the deleted wording. Git does not store Ultraglobal text.

## One sentence
Hybrid AI×ML brains swap **candidates per asset per timeframe** using methods adapted on owner CSV chart data, plus later news/macro/order-flow sockets. Losers go to a separate post-mortem brain. Execution stays on REGISTRY futures/spot. Candidates never become the tape.

## Brains (do not collapse into one net)

| Brain | Job | Exists now |
|---|---|---|
| A display | TradingView Pulse | Astra Pine |
| B execution | HistoryHub / Yahoo / Coinbase on REGISTRY | plant + engine |
| C candidate | Score AAPL…CAT vs NQ/ES/YM clock | Opus audit path |
| D trainers | Separate family per sampling process | `icarus-train` |
| E failure | Loser trades → why, so the same miss is not repeated | **not built** — next |
| F macro | CPI / FOMC / business prints as event features | FOMC dates only |
| G flow | Large-player volume / bookmap / true orderflow | **no licensed tape** |

Do not fake E/F/G from OHLC. Socket them when the owner adds a feed.

## Candidate swap rule
Per execution symbol × timeframe:
1. Strict screen (overlap ≥200 bars, Tide not all-zero, cluster agreement).
2. Anything that **beats** the screen stays eligible (owner: exceeding criteria is accepted).
3. Swap the active candidate set when agreement / RS / trainer holdout on **that** family degrades.
4. Never swap NQ execution for AAPL. Never size a stock with CME multipliers.

Clusters: `UNIVERSES.json` (`nq_candidates`, `es_candidates`, `ym_candidates`, plus metals/crypto complexes).

## CSV fuel
- Execution: `CME_*` / `CBOT_*` / `COMEX_*` / `NYMEX_*` / Coinbase / Bitstamp → `history/{SYM}_{tf}.csv`.
- Candidates: six batches in `reppiks490/multi-level-csv` (177 files, 17:32–17:48Z uploads) + earlier zips. `BATS_*` → `history/drop/candidates/` only.
- Families: minutes/hours/seconds/daily/weekly/renko/range/tick. Do not mix indexes.

## What is already finished (do not redo)
Catalog split, candidate cog, engine universe, trainer families, BATS quarantine, `ASTRA_DO_NOT`, `icarus-train`, trainer tests.

## What is not finished (continue here, not in a new Ultraglobal)
1. Opus candidate-audit JSON writer using `UNIVERSES.json`.
2. Brain E: journal losers → feature dump, no Pulse rewrite.
3. Wire FOMC calendar already in inputs as a binary feature on clock trainers (CPI when owner drops a dated CSV).
4. Astra replaces logit; keep family split and next-bar-on-this-index labels.
5. Export MBT, SOL, Coinbase ETHUSD, TPO/footprint when ready. Do not invent them.

## Recovery
- This file + Icarus commits today (`e27aa76` … `75b1b0e`).
- `multi-level-csv` six zip uploads + `CANDIDATE_COG_GROK.md`.
- Local video: ScreenSketch recordings `20260922-0358-29.6295002.mp4`.
- Not recoverable: the deleted Ultraglobal blob itself.

## Credit rule
Paste only: "Follow Icarus/GOAL.md. Obey ASTRA_DO_NOT.md. Continue unfinished items. Do not rebuild Pulse."
